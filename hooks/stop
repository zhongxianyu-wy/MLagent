#!/usr/bin/env python3
# T1 skeleton. T10 will wire: distill (if due) + git sync (commit+push) + UI stop.
import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parent.parent))

from mlagent.hooks import hook_output

hook_output("Stop", "mlagent session end. (skeleton — distill + sync wiring lands in T10.)")
